# Copy to .env and fill in. Backend works without a key (mock classification),
# but real AI classification + image generation require an OpenAI key.
OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-4o-mini
OPENAI_IMAGE_MODEL=gpt-image-1
